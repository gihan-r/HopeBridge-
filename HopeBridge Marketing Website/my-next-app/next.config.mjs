/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/HopeBridge-',
  assetPrefix: '/HopeBridge-/',
};

export default nextConfig;
